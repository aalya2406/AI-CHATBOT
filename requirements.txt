streamlit==1.21.0
requests==2.28.1
PyPDF2==3.0.1
paddlepaddle==2.6.1  # Adjust to the latest compatible version if 2.6.1 doesn't work
paddleocr==2.6.0  # Ensure compatibility with your paddlepaddle version
